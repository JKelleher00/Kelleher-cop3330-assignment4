package ucf.assignments;

import javafx.event.ActionEvent;
/*
 *  UCF COP3330 Summer 2021 Assignment 4 Solution
 *  Copyright 2021 John Kelleher
 */
public class Scene1 {
    public void AddNewClicked(ActionEvent actionEvent) {
        //Send to Scene 2
    }

    public void LoadPrevClicked(ActionEvent actionEvent) {
        //Make TitleAskerBox and Add button enabled, asking for name of list.
    }

    public void SaveAllExClicked(ActionEvent actionEvent) {
        //save each one in a text file
    }

    public void LoadSingleClicked(ActionEvent actionEvent) {
        //pop up a window to select one
    }

    public void LoadMultipleClicked(ActionEvent actionEvent) {
        //pop up a window to select multiple
    }

    public void FindClicked(ActionEvent actionEvent) {
        /*
        for every list that exists locally
            check if the name is the same
            if so exit and move to scene 2 with that list
            if none are the same show a text box calling them stupid or something idk
         */
    }
}
